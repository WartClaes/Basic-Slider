/**
 * Todo:
 * - Overrule default 'type' attribute
 * - 
 */

(function($) {
    $.fn.slider = function(settings, callback) {
        var $this = this;

        if(typeof callback !== 'undefined'){
            callback(errors);
        }
    };
}(jQuery));